%div#picture_frame_image{:class => 'picture_frame'}
  - photo = PictureFrame.currently_displayed
  - if photo.nil?
    = 'No photos to display'
  - else
    = link_to image_tag(photo.image.url(size)), album_photo_path(:album_id => photo.album.id, :id => photo.id)
